import { Routes } from '@angular/router';
import { FieldNetworkComponent } from '../field-network/fieldNetwork.component';

export default [
    {
        path: '',
        component: FieldNetworkComponent,
    },
] as Routes;
